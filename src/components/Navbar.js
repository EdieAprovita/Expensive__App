import React from 'react';

const Navbar = () => {
  return (
    <div className='d-flex justify-content-center'>
      <nav className='navbar navbar-dark bg-primary'>Navbar</nav>
    </div>
  );
};

export default Navbar;
